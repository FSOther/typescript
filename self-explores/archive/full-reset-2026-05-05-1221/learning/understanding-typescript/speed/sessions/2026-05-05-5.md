---
type: learning-session
date: 2026-05-05
course_slug: "understanding-typescript"
mode: "speed"
module: "M13"
lectures: ["L163","L164","L165","L166","L167","L168","L169","L170","L171","L172","L173","L174","L175","L176","L177","L178","L179","L180","L181"]
duration_minutes: null
---

# Learning Session — 2026-05-05 (Session 5)

## Goal
BUILD M13 DnD Project (L163-L181) theo speed mode: walk through resource progression, build full project, integrate TypeScript patterns from M06-M10.

## Materials Used

### Transcripts
- lectures/163-181 (19 files, M13)

### Resources
- resources/163-prj-00-initial-starting-setup/ → resources/179-prj-16-finished/ (15 incremental steps)

## Build Output

Project built at: `practice/dnd-project/`

**Compilation:** `./node_modules/.bin/tsc` → 0 errors ✅

---

## TypeScript Patterns Learned (Phase by Phase)

---

### Phase 1 — Form DOM Setup (L164-165)

**Pattern:** `document.getElementById()! as HTMLTemplateElement`
- `!` = non-null assertion (we KNOW it exists)
- `as Type` = type cast to tell TS the concrete type

```typescript
class ProjectInput {
  templateElement: HTMLTemplateElement;
  hostElement: HTMLDivElement;
  element: HTMLFormElement;

  constructor() {
    this.templateElement = document.getElementById('project-input')! as HTMLTemplateElement;
    this.hostElement = document.getElementById('app')! as HTMLDivElement;
    const importedNode = document.importNode(this.templateElement.content, true);
    this.element = importedNode.firstElementChild as HTMLFormElement;
    this.hostElement.insertAdjacentElement('afterbegin', this.element);
  }
}
```

**Active Recall:**
1. Q: Tại sao dùng `!` khi gọi `getElementById`? → A: TS không biết element có tồn tại không — `!` = non-null assertion, nói "tôi biết nó tồn tại"
2. Q: `document.importNode(template.content, true)` làm gì? → A: Clone template content vào DOM (true = deep clone)

---

### Phase 2 — @autobind Decorator (L166)

**Pattern:** Decorator để tự động bind `this` — tránh `.bind(this)` trong constructor

```typescript
function autobind(_: any, _2: string, descriptor: PropertyDescriptor) {
  const originalMethod = descriptor.value;
  const adjDescriptor: PropertyDescriptor = {
    configurable: true,
    get() {
      return originalMethod.bind(this);  // 'this' = instance calling the getter
    }
  };
  return adjDescriptor;
}

// Usage:
@autobind
private submitHandler(event: Event) { ... }
// Instead of: this.element.addEventListener('submit', this.submitHandler.bind(this));
```

**Active Recall:**
1. Q: `@autobind` decorator cần `experimentalDecorators: true` trong tsconfig không? → A: Có — thêm vào tsconfig.json
2. Q: Tại sao dùng getter trong adjDescriptor thay vì value? → A: Getter chạy khi property được accessed — `this` sẽ đúng context lúc đó

---

### Phase 3 — Validation Interface (L168)

**Pattern:** Interface để mô tả shape của validation config object

```typescript
interface Validatable {
  value: string | number;
  required?: boolean;
  minLength?: number;
  maxLength?: number;
  min?: number;
  max?: number;
}

function validate(input: Validatable): boolean {
  let isValid = true;
  if (input.required) {
    isValid = isValid && input.value.toString().trim().length !== 0;
  }
  // ... check minLength, maxLength, min, max
  return isValid;
}
```

**Active Recall:**
1. Q: Tại sao các props trong Validatable có `?`? → A: Optional — không phải lúc nào cũng cần check minLength hay min/max
2. Q: `input.minLength != null` tại sao dùng `!= null` thay vì `!input.minLength`? → A: Phân biệt minLength=0 (valid) với undefined — `!0` = true (wrong), `0 != null` = true (correct)

---

### Phase 4 — State Management + Listener Pattern (L170-171)

**Pattern:** Singleton + Observer pattern với generic listeners

```typescript
type Listener<T> = (items: T[]) => void;

class State<T> {
  protected listeners: Listener<T>[] = [];
  addListener(fn: Listener<T>) { this.listeners.push(fn); }
}

class ProjectState extends State<Project> {
  private static instance: ProjectState;
  
  static getInstance() {
    if (!this.instance) this.instance = new ProjectState();
    return this.instance;
  }
  
  addProject(title: string, desc: string, people: number) {
    const project = new Project(Math.random().toString(), title, desc, people, ProjectStatus.Active);
    this.projects.push(project);
    this.listeners.forEach(fn => fn(this.projects.slice()));
  }
}
```

**Active Recall:**
1. Q: Singleton pattern trong TS: tại sao constructor `private`? → A: Ngăn `new ProjectState()` từ outside — chỉ access qua `getInstance()`
2. Q: Tại sao truyền `this.projects.slice()` vào listeners thay vì `this.projects`? → A: Immutability — listeners nhận copy, không thể mutate internal state trực tiếp

---

### Phase 5 — Generic Abstract Component Class (L173)

**Pattern:** Abstract base class với 2 generic type params

```typescript
abstract class Component<T extends HTMLElement, U extends HTMLElement> {
  templateElement: HTMLTemplateElement;
  hostElement: T;
  element: U;

  constructor(templateId: string, hostElementId: string, insertAtStart: boolean, newElementId?: string) {
    this.templateElement = document.getElementById(templateId)! as HTMLTemplateElement;
    this.hostElement = document.getElementById(hostElementId)! as T;
    const importedNode = document.importNode(this.templateElement.content, true);
    this.element = importedNode.firstElementChild as U;
    if (newElementId) this.element.id = newElementId;
    this.hostElement.insertAdjacentElement(insertAtStart ? 'afterbegin' : 'beforeend', this.element);
  }

  abstract configure(): void;
  abstract renderContent(): void;
}
```

**Active Recall:**
1. Q: Tại sao `Component<T extends HTMLElement, U extends HTMLElement>`? → A: T = host element type (div/ul), U = rendered element type (form/section/li)
2. Q: Abstract methods `configure()` và `renderContent()` có body không? → A: Không — subclasses PHẢI implement, đây là interface contract thông qua abstract class

---

### Phase 6 — Drag & Drop Interfaces + implements (L176-178)

**Pattern:** Interface contract cho Draggable và DragTarget

```typescript
interface Draggable {
  dragStartHandler(event: DragEvent): void;
  dragEndHandler(event: DragEvent): void;
}

interface DragTarget {
  dragOverHandler(event: DragEvent): void;
  dropHandler(event: DragEvent): void;
  dragLeaveHandler(event: DragEvent): void;
}

class ProjectItem extends Component<HTMLUListElement, HTMLLIElement> implements Draggable {
  @autobind
  dragStartHandler(event: DragEvent) {
    event.dataTransfer!.setData('text/plain', this.project.id);
    event.dataTransfer!.effectAllowed = 'move';
  }
  // ...
}

class ProjectList extends Component<HTMLDivElement, HTMLElement> implements DragTarget {
  @autobind
  dragOverHandler(event: DragEvent) {
    if (event.dataTransfer?.types[0] === 'text/plain') {
      event.preventDefault();  // Allow drop
      this.element.querySelector('ul')!.classList.add('droppable');
    }
  }
  // ...
}
```

**Active Recall:**
1. Q: `event.dataTransfer!.setData('text/plain', id)` ở dragStart và `getData('text/plain')` ở drop — tại sao? → A: HTML5 drag API: setData lưu payload khi drag bắt đầu, getData lấy payload khi drop
2. Q: Tại sao cần `event.preventDefault()` trong `dragOverHandler`? → A: Mặc định browser không cho phép drop — preventDefault() báo hiệu element này có thể nhận drop

---

### Phase 7 — Getter (L175)

**Pattern:** `get` keyword cho computed properties

```typescript
class ProjectItem ... {
  get persons() {
    return this.project.people === 1 ? '1 person' : `${this.project.people} persons`;
  }
  renderContent() {
    this.element.querySelector('h3')!.textContent = this.persons + ' assigned';
    //                                                         ^ no ()
  }
}
```

**Active Recall:**
1. Q: Getter vs method — syntax khác nhau thế nào khi gọi? → A: Getter = `this.persons` (no parens); Method = `this.getPersons()` (with parens)
2. Q: Getter có thể nhận params không? → A: Không — getter = property accessor, không có params

---

## Feature Checklist (AC Verification)

- [x] Form input rendered (`#user-input` form visible)
- [x] Validation: title required, description minLength 5, people 1-5
- [x] Project list renders (ACTIVE PROJECTS + FINISHED PROJECTS sections)
- [x] State management: addProject → listeners fired → list re-rendered
- [x] Drag from active → finished (projectState.moveProject)
- [x] Visual drop feedback (`droppable` CSS class toggled on dragover/dragleave)

## TypeScript Concepts Integrated

| Concept | Where Used |
|---|---|
| Interfaces | Draggable, DragTarget, Validatable |
| Abstract class | Component<T, U> |
| Generics | Component<T, U>, State<T>, Listener<T> |
| Enum | ProjectStatus |
| Tuple return type | `[string, string, number] \| void` |
| Non-null assertion `!` | All DOM access |
| Type casting `as` | All DOM queries |
| Decorator | `@autobind` |
| Singleton pattern | ProjectState.getInstance() |
| `implements` | ProjectItem, ProjectList |

## Next Step
M13 hoàn thành. Task typescript-ml6 có thể đóng. Check `bd ready` for next task.
